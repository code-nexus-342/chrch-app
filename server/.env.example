# Copy this file to .env and replace the placeholders

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration
# Add your deployed frontend URL (e.g., https://your-app.netlify.app)
CLIENT_URL=https://your-frontend-url.com

# Database Configuration
# Neon PostgreSQL Database URL (or other PostgreSQL provider)
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# SMTP Email Configuration
# For Gmail: Enable 2FA and create App Password
# App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Admin Configuration
# Email that receives notifications and has admin privileges
RECEIVING_EMAIL=admin@atgchapel.org
ADMIN_EMAIL=admin@atgchapel.org
